extends layout

block content
  if out_content
    h1 Outputs
      blockquote
        pre
          code.
            #{out_content}
  h1 Download
  ul
    each f in files
      li
        a(href= "/download/" + f) #{f}
